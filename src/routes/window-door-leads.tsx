import { createFileRoute, Link } from "@tanstack/react-router";
import { DoorOpen, MapPin, CheckCircle2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { SiteFooter, SiteHeader } from "@/components/SiteHeader";
export const Route = createFileRoute("/window-door-leads")({head:()=>({meta:[{title:"Window & Door Leads for Contractors | Rivet Reach"},{name:"description",content:"Get matched window and door replacement leads through Rivet Reach based on project type and service area."}],links:[{rel:"canonical",href:"https://rivetreach.com/window-door-leads"}]}),component:WindowDoorLeadsPage});
function WindowDoorLeadsPage(){return <div className="min-h-screen bg-background"><SiteHeader/><main><section className="bg-steel text-steel-foreground"><div className="mx-auto max-w-6xl px-4 py-14 sm:py-20"><p className="text-base font-bold uppercase tracking-wide text-primary">Window and door leads</p><h1 className="mt-4 max-w-4xl font-display text-4xl font-bold leading-tight sm:text-6xl">Connect with homeowners planning window and door projects.</h1><p className="mt-6 max-w-3xl text-lg text-steel-foreground/85">Rivet Reach routes replacement and installation requests by service area so contractors can focus on the territory they actually cover.</p><Button asChild size="lg" className="mt-8 h-14 px-7 text-lg font-bold"><Link to="/login">Get Window & Door Leads</Link></Button></div></section><section className="mx-auto grid max-w-6xl gap-5 px-4 py-14 md:grid-cols-3">{[[MapPin,"Local territory","Choose where you want window and door opportunities."],[DoorOpen,"Relevant project types","Replacement windows, entry doors, patio doors, and related installation work."],[CheckCircle2,"Lead management","Track each opportunity through your contractor dashboard."]].map(([Icon,title,body])=>{const I=Icon as typeof MapPin;return <div key={String(title)} className="surface-card p-6"><I className="size-7 text-primary"/><h2 className="mt-4 text-xl font-bold">{String(title)}</h2><p className="mt-2 text-muted-foreground">{String(body)}</p></div>})}</section></main><SiteFooter/></div>}
